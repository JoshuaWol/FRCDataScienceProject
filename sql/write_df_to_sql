import pandas as pd
from data_science_db import get_sqlachemy_connection

def write_df_to_postgres ( data_frame: pd.DataFrame, sql_table_name: str, if_exists_action : str = "append") -> None:
    with get_sqlachemy_connection() as sql_conn:
        df.to_sql(
            name = sql_table_name,
            con = sql_conn,
            if_exists = if_exists_action,
            index = False,
            method = 'multi',
            chunksize = 300

        )